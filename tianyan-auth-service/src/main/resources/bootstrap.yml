spring:
  application:
    name: auth-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 192.168.136.210:8848
        file-extension: yml
        namespace: dev-hlj
        group: tianyan-cloud
        prefix: ${spring.application.name}

