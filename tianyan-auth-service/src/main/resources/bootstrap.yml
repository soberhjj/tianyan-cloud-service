spring:
  application:
    name: auth-service
  profiles:
    active: test
  cloud:
    nacos:
      config:
        server-addr: 192.168.136.210:8848
        file-extension: yml
        namespace: tianyan-cloud
#        group: hlj
        prefix: ${spring.application.name}

